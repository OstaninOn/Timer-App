# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Timer App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Timer App

  target 'Timer AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Timer AppUITests' do
    # Pods for testing
  end
pod 'lottie-ios'
end
